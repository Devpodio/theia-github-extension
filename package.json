{
  "private": true,
  "scripts": {
    "prepare": "npm run snyk-protect && lerna run prepare",
    "rebuild:browser": "theia rebuild:browser",
    "rebuild:electron": "theia rebuild:electron",
    "publish:latest": "lerna publish --registry=https://npm.devpod.io/ --skip-git --force-publish",
    "publish:next": "lerna publish --registry=https://npm.devpod.io/ --exact --canary=next --npm-tag=next --force-publish --skip-git --yes",
    "snyk-protect": "snyk protect"
  },
  "devDependencies": {
    "lerna": "^2.11.0",
    "rimraf": "latest",
    "typescript": "latest"
  },
  "workspaces": [
    "github-extension/*",
    "browser-app/*",
    "electron-app/*"
  ],
  "dependencies": {
    "@octokit/rest": "15.2.5",
    "snyk": "^1.230.5"
  },
  "snyk": true
}